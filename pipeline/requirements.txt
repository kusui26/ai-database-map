# AI Database Map — pipeline（Python）の依存
# インストール: python3 -m pip install -r pipeline/requirements.txt
pandas>=2.3
psycopg[binary]>=3.3
requests>=2.32     # e-Stat API（fetch_income / fetch_working_age_mesh）
openpyxl>=3.1      # 総務省 xlsx の読み取り（fetch_income）
geopandas>=1.1      # 原典（シェープファイル）の読み取り（build_hazard_mesh）
rasterio>=1.4      # ポリゴン → 250m 格子のラスタ化（build_hazard_mesh）
pillow>=11.0       # 公式タイルの画素照合（validate_hazard_mesh --tiles）
